import styled from "styled-components/native";
import { colors } from "../../theme/custom-theme";

export const Layout = styled.ScrollView`
    background-color: ${colors.scalesGrey.gris_30MB};
    padding: 20px;
    margin-top: 40px;
`;
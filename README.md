# Points app 
It is recommended to use version node v16
## Installation
### Install dependences
`
npm install
`
### Run project
Android:
`
npx expo start --android
`
IOS:
`
npx expo start --ios
`
##  Run tests
`
npm Run test
`